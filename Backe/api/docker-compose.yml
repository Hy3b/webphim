services:
  redis:
    image: redis:7.2-alpine
    container_name: webphim-redis
    ports:
      - "6380:6379"
    volumes:
      - redis_data:/data
    command: redis-server --save 60 1 --loglevel warning --requirepass ${REDIS_PASSWORD:-webphim-secret-redis}

volumes:
  redis_data:
